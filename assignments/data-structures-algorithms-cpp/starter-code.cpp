// Código Inicial para a Tarefa de Estruturas de Dados e Algoritmos em C++
#include <iostream>
#include <vector>

// Tarefa 1: Busca Linear
int linearSearch(const std::vector<int>& values, int target) {
    // Percorrer o vetor e retornar o índice do alvo, ou -1 se não encontrado
    return -1;
}

// Tarefa 2: Busca Binária
int binarySearch(const std::vector<int>& values, int target) {
    // Implementar a busca binária assumindo vetor ordenado
    return -1;
}

// Tarefa 3: Ordenação e Integração
void insertionSort(std::vector<int>& values) {
    // Ordenar o vetor in-place em ordem crescente
}

int main() {
    std::vector<int> numbers = {8, 3, 1, 9, 5, 2, 7};

    // Use as funções acima para testar busca linear, ordenação e busca binária

    return 0;
}
